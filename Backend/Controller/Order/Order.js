import httpStatus from "http-status";
import Order from "../../Model/Order";

export const CreateOrder = async (req, res) => {
  const { address, items, totalPrice, orderDate, phoneNumber } = req.body;
};

try {
    
} catch (error) {
    
}